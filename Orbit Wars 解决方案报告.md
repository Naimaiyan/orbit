# Kaggle - Orbit Wars 解决方案报告

---

## 1. 竞赛背景与问题定义

**这道题在解决什么问题。** Orbit Wars（轨道战争）是 Kaggle 的一场**仿真对抗赛**：参赛者交的不是 CSV 预测表，而是一个能持续决策的 Python bot（自动决策程序）。平台把多个 bot 放进同一张太空地图对战，让它们生产飞船、发射舰队、争夺星球；在大量对局里稳定获胜的 bot，天梯评分就更高。比赛有两个赛制：**2P**（1v1）和 **4P FFA**（四人自由混战）。

**输入是什么、输出是什么。** 游戏按离散的「步」推进，每一步平台给 bot 一份**观测**（observation，当前所有星球、在途舰队、步数、自己的玩家编号等），bot 返回一个**动作列表**，每个动作形如：

```python
[出发星球ID, 发射方向(弧度), 发射飞船数]
```

一个关键约束贯穿整个方案：**你只能给方向和数量，不能点名攻击某颗星球**。舰队沿该方向直线飞行，飞进哪颗星球的半径就参与哪颗的战斗。可以一步发多支，也可以不发（等待）。

**为什么这道题对 ML 很难。** 至少三点：

1. **决策是序列化、长时程的。** 一局最多 500 步，早期送兵扩张会影响几十步后的战局，属于典型的强化学习（RL，让智能体在与环境交互中学策略）问题，没有现成标准答案可监督。
2. **动作空间大且带几何。** 「从哪颗星球、朝哪个连续角度、发多少艘」组合爆炸；还要处理移动星球（需预判未来位置）、太阳摧毁、同步到达的战斗结算。
3. **对手会变。** 天梯上的对手风格各异，没有固定「正确对手」；策略必须对多种对手都稳健。

---

## 2. 胜负判定与天梯评分

这是一场仿真赛，**没有传统监督学习的 loss 或评估公式**。它的「评估」分两层，来源不同，必须分清：

**① 单局胜负（官方规则，以 `orbit_wars.py` 为准）。** 一局最多 500 步，满足任一条件即结束：

- 场上只剩一位玩家还有飞船（星球驻军 + 在途舰队都算）；或
- 到达 500 步上限。

走到上限时，比较每位玩家的**总飞船数 = 星球驻军 + 正在飞行的舰队**，最多者获胜。由此可推出两条策略含义：终局前把船乱送进太阳/界外会直接掉总分；只占很多低兵力星球也不保证赢。

**② 天梯评分（平台机制，属背景资料）。** 平台用高斯分布的 **Skill Rating**（技能评分，用均值 μ 和不确定度 σ 刻画一个 bot 的真实实力）给每个 bot 打分，多局对战后不断更新。

**方案是直接优化这个指标，还是走间接路线？** 走**间接路线**。天梯分无法直接求梯度，所以训练时优化的是一个代理信号：每一步用「局面绝对评分」当塑形奖励（shaped reward），一局的最后一步再换成环境给的**真实胜负**，交给 PPO 学习（详见 §5 阶段四）。也就是说，网络从没直接「见过」天梯分，它是通过「多赢局、赢得干净」间接把天梯分做上去的。

---

## 3. 「数据」从哪来：没有静态数据集

传统 Kaggle 赛有 `train.csv` / `test.csv`。**这场赛没有任何静态数据集**——训练数据是**自我对弈实时生成**的：官方环境负责执行游戏规则，方案让当前网络和自己的历史版本反复对打，边打边产生「观测 → 动作 → 下一观测」的经验。所以这一节要讲的不是「数据文件」，而是**网络实际消费的观测结构**。

每一步观测里，方案真正用到的两张表：

| 字段来源 | 每行/每项结构 | 方案怎么用 |
| --- | --- | --- |
| `planets`（星球） | `[id, owner, x, y, radius, ships, production]`，`id<0` 表示死槽位 | 编成每颗星球 14 维基础特征；`owner/ships` 用来挑来源、算兵力差 |
| `fleets`（在途舰队） | `[id, owner, x, y, angle, from_planet_id, ships]` | **只**在算奖励（兵力差）时用到 `owner`(第1列)、`ships`(第6列) |
| `step` / `player` | 标量：当前步数、自己的玩家编号 | `step/500` 进全局特征；`player` 定「我方/敌方」视角 |

---

## 4. 解决方案架构

一句话概括核心思想：**官方环境负责全部游戏规则；神经网络只在一个被刻意收窄的动作集合里选目标；PPO 靠历史对手池自我对弈、从随机权重训起。** 训练端（PyTorch）与提交端（纯 NumPy）通过一份导出的权重文件对接：

各阶段分工：**官方环境**执行规则、结算战斗、给观测与胜负；**特征层**把观测压成网络能吃的数字；**网络**给每个候选动作打分；**动作契约**把打分还原成合法动作；**PPO** 把对局经验变成更好的权重；**导出**把训练权重转成提交端能读的格式；**提交端**在 Kaggle 上不依赖训练框架、只用 NumPy 跑一遍前向做决策。

这里有一道容易被忽略的**接缝**：训练端存的是 `.pt`（含权重 + 优化器状态，供续训），而提交端**刻意不 import torch**、只读 `.npz`。所以 `.pt` 不能直接上传，必须转换——转换由训练脚本在**刷新 best 时**自动完成，把 `state_dict` 里每个张量转成 NumPy 数组写进 `submission_*/model_weights.npz`（键名保持模块路径不变）。因此 `submission_*/` 目录训练完就是一个可直接打包上传的完整包。

---

## 5. 各阶段详细解析

### 阶段一：观测 → 特征（`extract_state`）

- **目标**：把一帧观测变成三路定长张量，同时落实动作契约里「选谁当来源、谁当候选」的部分。
- **方法**：一帧观测里每颗星球是一行 `[id, owner, x, y, radius, ships, production]`。`extract_state` 把它拆成**三路定长张量**，分别回答三个问题——**从哪出发**（来源）、**能打谁、打过去多贵**（候选目标）、**眼下大局如何**（全局）。其中**来源特征**和**每个候选目标的前 14 维**都出自同一个 `planet_features` 函数（同一套「星球基础特征」，只是喂进去的星球不同）：

  **A. 星球基础特征表（`SELF_DIM=14`）**——描述「一颗星球本身长什么样」，是下面两路共用的**底料**：把它套在**驻军最多的来源星球**上，得到的 14 维就是**来源特征**；套在**每个候选目标**上，就是**候选特征的前 14 维**。2P / 4P 逐位一致：

  | 维 | 含义 | 归一化 |
  | --- | --- | --- |
  | 0–1 | 星球坐标 (x, y) | ÷ 棋盘边长 100 |
  | 2 | 半径 | ÷ 5 |
  | 3 | 驻军（当前飞船数） | 截到 500 后 ÷ 500 |
  | 4 | 生产值（每步产多少兵，1–5） | 截到 20 后 ÷ 20 |
  | 5 | 离棋盘中心（太阳）的距离 | ÷ 70.7（角到中心的最大距离） |
  | 6 | 是否在绕太阳的轨道上 | 0 / 1 |
  | 7–8 | 从星球指向棋盘中心的方向 (dx, dy) | ÷ 100 |
  | 9 / 10 / 11 | 归属独热：我方 / 敌方 / 中立 | 各 0 / 1 |
  | 12 | 绕行轨道半径（不绕行填 0） | ÷ 100 |
  | 13 | 是否存活 | 0 / 1 |

  **B. 候选特征 ＝ 上面 14 维 ＋ 额外 4 维几何（第 14–17 维 ⇒ `CAND_DIM=18`）**——多出的这 4 维描述「从选定的那颗来源，打向这颗目标」的代价：

  | 维 | 含义 | 归一化 |
  | --- | --- | --- |
  | 14–15 | 从来源指向该目标的方向 (dx, dy) | ÷ 100 |
  | 16 | 来源到该目标的直线距离 | ÷ 对角线 141.4 |
  | 17 | 粗略 ETA（预计到达步数）= 距离 ÷ 该来源舰队速度 | 截到 50 后 ÷ 50 |

  **C. 全局特征（`GLOBAL_DIM`）**——独立的第三路，一把描述整盘局势的数，2P 与 4P 不同：
  - **2P（8 维）**：步数进度（`step/500`）＋ 我方 / 敌方 / 中立**星球数**（各 ÷64）＋ 我方 / 敌方**总驻军**（各 ÷32000）＋ 我方 / 敌方**总生产**（各 ÷1280）。
  - **4P（16 维）**：步数进度 ＋ **自己与三个对手各 3 项**（星球数、总驻军、总生产，共 12）＋ **三个对手各自是否还有星球**（3 个 0/1）。

  **真正喂进网络的是这三路张量**：**来源（14）/ 候选（18 ＝ 14 ＋ 4）/ 全局（8 或 16）**，各过一个独立编码器，到网络内部才汇合（见阶段二）。三路之间**不相加**；候选那一路的「14 ＋ 4」也是**首尾拼接**、不是数值相加。这里最容易误会的一点：**A 和 B 的前 14 维是同一张表、却填的是不同星球**——A 填的是「来源那颗」，B 前 14 维填的是「每个候选目标」，算出来的数值不同，**不是把同一份特征抄了两遍**。网络正是靠「来源什么样（A）＋ 目标什么样（B 前 14）＋ 两者的几何关系（B 后 4）」才能判断该从来源打向哪个目标。
- **关键决策**：来源门槛是驻军 `≥4`（太少不值得发兵）；无可行动作时返回 `None`（这步等待）。
- **注意事项**：这里的每一位归一化除数（`BOARD_SIZE`、`MAX_SHIPS=500`、`MAX_PROD=20`、`70.7≈半对角线`…）都**必须与提交端逐位一致**，否则线上行为会偏离训练。

### 阶段二：特征 → 打分（Actor-Critic 网络）

- **目标**：给每个候选动作打一个分（策略），同时估计当前局面的价值。
- **方法**：整张网络由两块小积木搭成——
  
  - **残差块（ResidualBlock）**：`y = x + (LayerNorm → Linear → ReLU → Linear)`。进出都是 256 维，「加回自身」让深层也好训练。
  - **编码器（Encoder）**：`Linear(输入维 → 256) → 2 个残差块`。把某一路输入压成统一的 **256 维**隐向量；三路输入各配一个**独立**编码器。
  
  三路编码后**拼装**、再送两个**输出头**。2P（`TargetPolicy`）的完整数据流如下（`N` = 候选目标数；形状省略最前面的批大小维）：

```
① 三路输入          ② 三个独立编码器          ③ 编码后隐向量
来源特征 [14]   →  self_encoder       →  self_h   [256]
候选特征 [N,18] →  candidate_encoder  →  cand_h   [N,256]
全局特征 [8]    →  global_encoder     →  global_h [256]

④ 拼装：self_h、global_h 各复制 N 份，与每个候选的 cand_h 首尾相接
        每个候选 → [ self_h | global_h | cand_h ] = 768 维 → joint [N,768]

⑤ 两个输出头（各是 768→256→128→1 的 3 层 MLP，都读上面同一套编码器输出）
   ├ target_head(joint) → 每个候选 1 分 [N]（再 ×logit_scale、非法目标填 -inf）→ 策略分数 [N]
   └ value_head( self_h | global_h | cand_h 的均值 ) → 局面价值 [1]
```

  策略分数在 N 个候选上做 softmax，就是「该打哪一颗目标」的概率；价值就是对当前局面好坏的打分。

- **4P（`FourPlayerPolicy`）在此之上有三处不同**：
    - **来源变多个**：取驻军最多的**最多 4 颗**来源 → `source_h [S,256]`；候选是「来源×目标」网格 → `cand_h [S,T,256]`。
    - **多一个「等待」动作**：`hold_head( 各来源隐向量均值 | 全局 )` 出 1 个 hold 分；最终把 `S×T` 个格子分数**拉平、末尾接上 hold 分**，构成长度 `S×T+1` 的统一分类分布（最后一位＝等待）。pair 与 hold 共用同一个 `logit_scale`，`value_head` 同理用各路隐向量的均值汇总。


- **关键决策**：这是 **Actor-Critic**（演员-评论家：演员出动作分布，评论家估局面价值，二者共享编码器）。`logit_scale` 只在训练采样时影响各动作相对概率；提交端贪心 `argmax` 下，正数缩放不改变谁最大，所以它对线上选择无影响，保留只为两端逐位一致。
- **注意事项**：网络输入就是阶段一那三路特征——**不含在途舰队、不含彗星、不做路线/战斗预测**。方案的「智能」上限由这份输入决定。

### 阶段三：打分 → 动作（动作契约）

这是本方案最有辨识度的设计，2P 与 4P 不同：

| | 2P（`TargetPolicy`） | 4P（`FourPlayerPolicy`） |
| --- | --- | --- |
| 来源 | **只**取己方驻军最多的 **1 颗** | 取驻军最多的**最多 4 颗** |
| 选择 | 在其余存活星球里选 1 个目标 | 在「来源×目标」组合里选 1 个，**或**显式选 `hold`（等待） |
| 发射 | 全军出击（把来源的兵全发出去） | 选中组合则全军出击，选中 hold 则不发 |
| 等待 | 无显式等待项（有可行来源就必发） | 有显式 hold 分支（`hold_head`，与 pair 共用温度） |

- **目标**：把「连续角度 × 任意星球 × 任意兵数」的巨大动作空间，压成「在有限个候选里做一次分类选择」，让 PPO 在小得多的空间里学得动。
- **关键决策/代价**：方向不是自由角度，而是**当前指向某颗候选星球的角度**（`atan2(dy, dx)`）；兵数固定为「全军」。这换来了可学性，代价是放弃了「精确拦截移动目标」「留守分兵」这类更细的操作——这是方案的已知边界，如实列出，不夸大成已实现的高级能力。

### 阶段四：PPO 自我对弈训练

- **目标**：从随机权重开始，只靠自我对弈把策略练强。
- **方法**：
  - **收集经验**：每个迭代自我对弈若干局（2P 每迭代 8 局，4P 4 局），对手从**历史对手池**（容量 4 的队列）里随机抽一个版本，座位轮换以抵消先后手偏差。
  - **奖励**：每步用 `shaped_reward` 打「下一状态的绝对局面分 = 兵力差 + 星球数差」，其中**兵力 = 星球驻军 + 在途舰队**（与官方计分口径一致，发出的大舰队不会被误判成丢兵）；一局最后一步换成环境的**真实胜负**奖励。
  - **优势估计**：`compute_gae` 用 **GAE**（广义优势估计，γ=0.99、λ=0.95）把每步的即时惊喜按时间衰减累加成「优势」，`returns = 优势 + 价值` 作为价值头的回归目标。
  - **更新**：`ppo_update` 用 **PPO**（近端策略优化，clip ε=0.2）多轮小批量更新；损失 = 截断策略损失 + 0.5·价值损失 − 0.01·熵（熵项鼓励探索）；梯度裁剪上限 0.5。
- **关键超参**（`Config`，2P/4P 除下表外一致）：

  | 超参 | 值 | 含义 |
  | --- | --- | --- |
  | `total_iters` | 2000 | 总迭代数 |
  | `games_per_iter` | 8 (2P) / 4 (4P) | 每迭代自我对弈局数 |
  | `batch_size` | 256 (2P) / 128 (4P) | PPO 小批量 |
  | `hidden_size` / `num_blocks` | 256 / 2 | 编码器宽度 / 残差块数 |
  | `lr` | 3e-4 | Adam 学习率 |
  | `gamma` / `gae_lambda` | 0.99 / 0.95 | 折扣 / GAE λ |
  | `clip_eps` | 0.2 | PPO 截断范围 |
  | `opponent_pool_size` | 4 | 历史对手池容量 |
  | `eval_interval` / `eval_games` | 25 / 24 | 每 25 迭代评估、每次 24 局 |
  | `seed` | 20260711 | 随机种子 |

- **评估与存档**：每 25 迭代让当前模型与 best 贪心对打 24 局；胜率达标（2P `≥0.55`，4P `≥0.45`，因四人局随机基线约 25%）才替换 best、存 `best.pt` 并导出提交权重。每次评估还存 `latest.pt`，训练末尾存 `final.pt`。

### 阶段五：权重导出（`.pt → .npz`）

- **目标**：把训练权重转成提交端能直接读的格式。
- **方法**：`export_submission_weights()` 把 `state_dict` 每个张量转成 NumPy 数组、`np.savez` 写进对应 `submission_*/model_weights.npz`，**键名保持模块路径**（如 `self_encoder.input_proj.weight`）不变。
- **关键决策**：只在**刷新 best 时**导出，所以 `submission_*/` 里的权重始终等于「当前最好的模型」；导出前 `mkdir` 父目录，便于把训练脚本挪到别处（如 Colab）跑。
- **注意事项**：仓库**不附带**任何 `.pt`/`.npz`，所以提交端要先跑一次训练（且训练需装 `kaggle_environments`）才有权重可用。

### 阶段六：纯 NumPy 推理（提交端）

- **目标**：在 Kaggle 受限环境里，不依赖训练框架就能加载权重、读观测、出动作。
- **方法**：`submission_*/main.py` 刻意**不 import torch / kaggle_environments**，把线程数锁成 1，从同目录 `model_weights.npz` 读权重，用 NumPy 手写 `_linear`（注意要对权重转置 `.T`，因为 `nn.Linear` 存成 `[out, in]`）、`_layer_norm`（eps=1e-5 要与 PyTorch 默认一致）、`_encoder`、`_target_head`/`_mlp`，最后**贪心 `argmax`** 选动作（不采样）。
- **关键决策**：`agent()` 外层用 try/except 兜底——出任何意外返回合法的**空动作**（这步不发兵）而非抛错，因为抛错会直接判负。
- **注意事项**：提交端的特征与前向必须与训练端**逐位一致**，否则线上行为偏离；两端**无自动校验**，靠人工把关（见 §6 最后一条）。

---

## 6. 关键技术与创新点

**① 收窄的动作契约。** 把「连续角度 × 任意星球 × 任意兵数」压成「在有限候选里选一次目标、全军出击」。为什么有效：RL 在巨大连续动作空间里几乎学不动，收窄后策略网络只需做一个分类选择，探索难度骤降，从随机权重也能训起来。这是让方案「可训练」的第一功臣。

**② 从随机权重自我对弈 + 历史对手池。** 不用任何人类棋谱或预训练，让网络和自己的历史版本对打，边打边强。为什么有效：对手池（容量 4，每 25 迭代存一版）+ 座位轮换，避免网络只会打「最新的自己」而过拟合单一对手，让策略更稳健。

**③ 与官方计分对齐的塑形奖励。** 每步奖励 = 兵力差 + 星球数差，且**兵力把在途舰队也算进去**，末步再换成真实胜负。为什么有效：稀疏的「最后赢没赢」很难学；用「局面持续变好」当密集代理信号能加速学习，而把在途舰队计入兵力，避免「刚发出大舰队」被误判成实力下降，奖励口径与最终胜负一致。

**④ 零依赖的纯 NumPy 推理链路。** 提交端不带 torch，手写前向从 `.npz` 读权重。为什么有效：提交包更轻、启动更快、不受训练框架版本影响；配合「刷新 best 即导出」，训练完 `submission_*/` 就是可直接上传的完整目录。

**⑤（4P 专属）多来源 + 显式 hold。** 4P 把来源从 1 颗扩到最多 4 颗，并允许显式「等待」。为什么有效：四人混战里「按兵不动、让另外两家先火并」常常是最优解，显式 hold 让网络能主动选择不出手，而不是被迫每步发兵。

> 一条贯穿全方案的工程纪律：训练端与提交端的**特征布局和前向计算必须逐位一致**，且当前**没有自动校验脚本**——任何一侧改了特征维度、网络结构或归一化除数，都必须人工同步四个文件（2 训练 + 2 提交）。这是选择「轻量零依赖提交端」换来的代价，写在这里以免后续维护踩坑。

---

## 7. 简历项目经历模板

```
Orbit Wars - 太空即时策略对抗 bot（PPO 自我对弈）| Kaggle 竞赛 | 【2026.05 – 2026.07】

· 任务：为 Kaggle 实时策略仿真赛（2P / 4P FFA）设计自主决策 bot，在含太阳、移动星球与彗星的 100×100 地图上生产飞船、发射舰队、争夺星球，以天梯 Skill Rating 排名（银牌方案，天梯分【xxxx】/ 名次【xx】/【x】队）。
· 状态表征：将官方观测编码为星球基础特征(14维)、候选目标几何特征(18维)与全局特征(2P 8维 / 4P 16维)，经三路残差编码器(LayerNorm + 残差块)与 Actor-Critic 双头输出目标打分与状态价值。
· 强化学习：用 PPO(GAE γ=0.99/λ=0.95、截断 ε=0.2)从随机权重自我对弈训练，依托容量 4 的历史对手池与座位轮换稳定训练，评估胜率达标才替换最优模型。
· 动作设计：以「最富来源→选目标→全军出击」的收窄动作契约压缩巨大动作空间(4P 扩展为多来源 + 显式 hold)，配合与官方计分对齐、计入在途舰队的塑形奖励，显著降低探索难度。
· 工程：提交端用纯 NumPy 手写前向(不依赖 PyTorch / kaggle_environments)，从 .npz 权重贪心推理并异常兜底；训练端刷新最优即导出提交权重，训练完即可打包上传。
```
